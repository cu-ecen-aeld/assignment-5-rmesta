
config BR2_PACKAGE_AESD_ASSIGNMENTS
	bool "aesd-assignments"
	help
	  Includes the native sockets server executable and related init.d script
	  into the kernel image. Used to understand buildroot external trees and
	  package builds.
